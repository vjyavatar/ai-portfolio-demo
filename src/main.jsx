import React from "react";
import ReactDOM from "react-dom/client";
import CelesysApp from "./celesys_app.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CelesysApp />
  </React.StrictMode>
);
